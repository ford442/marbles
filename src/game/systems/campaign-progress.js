/** Compatibility shim — implementation lives in `campaign-progress.ts`. */
export {
    CampaignProgress,
    CAMPAIGN_STORAGE_KEY,
    mergeCampaignSave,
    mergeLevelProgress,
} from './campaign-progress.ts';

export { default } from './campaign-progress.ts';
