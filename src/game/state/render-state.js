/** Filament / engine handles (populated during init). */
export function createRenderState() {
    return {
        Filament: null,
        material: null,
        cubeMesh: null,
        cueInst: null,
    };
}
